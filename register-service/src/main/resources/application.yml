server:
  port: 8761
spring:
  application:
    name: register-server
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
      #defaultZone: http://localhost:8767/eureka/,http://localhost:8766/eureka/